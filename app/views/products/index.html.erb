<div class="container mt-5">
  <h1>AQUI E A PAGINA DE PRODUTOS</h1>
  <div class="row">
    <% @products.each do |product| %>
      <div class="col-4">
        <div class="food-card">
       <%= cl_image_tag product.photo.key, height: 200, width: 400, class: "food-card-image", crop: :fill %>
        <h3 class="text-center"><%= link_to product.title, product_path(product) %></h3>
        <h4>
        </div>
      </div>
    <% end %>
  </div>

  <%= link_to 'create new product', new_product_path(@product)%>
</div>
