<nav class="navbar navbar-expand-sm fixed-top p-0" data-controller="navbar" data-action="scroll@window->navbar#updateNavbar">
  <div class="collapse navbar-collapse d-flex" id="navbarNav">
    <%= link_to root_path do %>
     <%= image_tag 'logo_medio.png', size: '80x80' %>
    <% end %>
    <ul class="navbar-nav">
      <li class="nav-item">
        <%= link_to 'All products', products_path, class: 'nav-link' %>
      <% if user_signed_in? %>
        <li class="nav-item dropdown">

        <%= cl_image_tag current_user.avatar.key, id: "navbarDropdown", "data-bs-toggle": "dropdown", class: "avatar dropdown-toggle",  height: 40, width: 40 %>
        <div class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdown"> <p><strong>Hello, <%= "#{current_user.name.capitalize}!" %></strong></p>
          <%= link_to 'Edit', edit_user_registration_path, class: 'dropdown-item' %>
          <%= link_to 'Logout', destroy_user_session_path, method: :delete, class: 'dropdown-item' %>
        </div>
      <% else %>
        <%= link_to 'Login', new_user_session_path, class: 'nav-link' %>
      <% end %>
      </li>
    </ul>
  </div>
</nav>
